---
title: Corpo
description: Cloned from path:./README.md.
remote: path:./README.md
syncedAt: '2026-03-11T09:10:35.647Z'
checksum: 11ab81b7c9ca86638d45acc71479d451b49c785a0bf8ab9bb3ac09e177cbd50a
threads:
  81b99319:
    author: me
    timestamp: '2026-03-11T09:14:38.056Z'
    body: |-
      I don't feel like this is a fitting title anymore. Corpo has two main use cases I'm noticing:
      1. Agent-powered drafting experience
      2. Filesystem optimized for agent context

      To compliment (1), we also include self-hosting via github pages for OOTB links to share with teammates that they can comment on to complete the iteration loop of:
      1. author drafts doc
      2. author shares doc
      3. team reviews doc and iterates

      To compliment (2), we are leaning into agent-centric paradigms like local-first, markdown files with a git backend and federated wiki via file cloning.
    replies:
      - author: claude
        timestamp: '2026-03-11T09:25:50.442Z'
        body: 'Done: rewrote the intro tagline and description to lead with the two use cases — agent-powered drafting loop and local-first agent context filesystem.'
      - author: me
        timestamp: '2026-03-11T09:29:51.208Z'
        body: I change my mind on framing. Corpo is a context-management layer. It emphasizes agent-first collaboration on the defining context for working on project teams. It is optimized for managing a wiki-as-context easily across distributed project repositories. It provides an agent-driven drafting process with GUI for intuitive human operating models like comments.
  2010ad7f:
    author: me
    timestamp: '2026-03-11T09:18:38.311Z'
    body: Don't think this is necessary for README. Users and agents can learn on the fly after installation. We still need to sell people on value to want to try this tool.
    replies:
      - author: me
        timestamp: '2026-03-11T09:23:33.260Z'
        body: |-
          Let's replace this section with "Features". Core features are:
          1. Agent-ready collaborative GUI
          2. Self-hosted pages for sharing
          3. Full markdown support + comment threads
          4. Cross-repository wiki building with cloning primitives
      - author: claude
        timestamp: '2026-03-11T09:26:51.307Z'
        body: 'Done: replaced Commands table with a Features section covering the 4 core features.'
  8052d9d5:
    author: me
    timestamp: '2026-03-11T09:23:47.119Z'
    body: Remove section
  b10027cf:
    author: me
    timestamp: '2026-03-11T09:24:03.031Z'
    body: Remove section
  4782bcd4:
    author: me
    timestamp: '2026-03-11T09:24:09.672Z'
    body: Remove section
---
# Corpo

An agent-ready document layer for your git repo — built for two things: drafting with your agent and giving teammates a place to review.
<!-- thread:81b99319 -->

Write docs with your agent, publish them as hosted pages teammates can comment on, and let your agent close the loop. Under the hood, corpo is a local-first filesystem optimized for agent context: plain Markdown files, a git backend, and cross-repository sharing via cloning primitives.

---

## Install

**For agents** — install the corpo skill from the official repo:

```bash
npx skills add github.com/ilikesymmetry/corpo
```

**For humans** — install Bun and the corpo CLI:

```bash
# 1. Install Bun (skip if already installed)
curl -fsSL https://bun.sh/install | bash

# 2. Install corpo
npm install -g @ilikesymmetry/corpo
```

---

## Quick start

**For agents** — once the corpo skill is installed, say:

```
/corpo set me up
```

The skill will initialize corpo in your current repo, create a starter file, and get you ready to draft.

**For humans:**

```bash
# in any git repo (or create a new one)
git init my-repo && cd my-repo

# initialize corpo
corpo init

# create your first file
corpo new "My first doc" "A short description"

# browse it
corpo serve    # http://localhost:3000
```

---

## Workflow

The loop that works:

1. Chat with your agent about something you want to write
2. Ask the agent to write it to a corpo file
3. Open the GUI (`corpo serve`) to view the draft
4. Highlight text to leave comments on the draft
5. Ask the agent to resolve threads (`/resolve-threads`)
6. Review the agent's changes and replies, resolving threads as you go
7. When satisfied, ask the agent to do its own review (`/review`)
8. Reply to the agent's comments
9. Ask the agent to resolve threads again
10. Share the GitHub Pages link with teammates for their review

---

## Features
<!-- thread:2010ad7f -->

1. **Agent-ready collaborative GUI** — browse, draft, and comment on docs with your agent via a local server or GitHub Pages
2. **Self-hosted pages for sharing** — any corpo repo gets a hosted GUI on GitHub Pages, no backend required
3. **Full Markdown support + comment threads** — threads attach to any block of content and live alongside the doc in git
4. **Cross-repository wiki building** — clone files from other repos, pull updates, and push changes back with `corpo clone / pull / push`

---

## Structure
<!-- thread:8052d9d5 -->

```
.corpo/
  config.json         # navigation tree, remote file refs
  files/
    <32-char-hex-id>.md
```

`config.json` example:

```json
{
  "navigation": [
    "a1b2c3d4e5f64a7b8c9d0e1f2a3b4c5d",
    {
      "group": "Architecture",
      "children": [
        "f1a2b3c4d5e64f7a8b9c0d1e2f3a4b5c"
      ]
    }
  ]
}
```

---

## Development

**Prerequisites:** [Bun](https://bun.sh) >= 1.0 and git.
<!-- thread:b10027cf -->

```bash
bun install   # installs all workspace packages (packages/cli and packages/gui)
```

**Dev mode** — both servers in one command:

```bash
bun run dev
```

Or separately if you need independent control:

```bash
# Terminal 1 — API server (port 3000)
bun run dev:cli

# Terminal 2 — GUI with HMR (port 5173, proxies /api/* to port 3000)
bun run dev:gui
```

The Vite dev server runs at `http://localhost:5173`. Use this URL when working on GUI source — it hot-reloads on every change. `http://localhost:3000` serves the last built dist and does not hot-reload.

**Build:**

```bash
bun run build

# or step by step from packages/cli:
bun run build:gui   # builds GUI dist and copies to packages/cli/gui/
bun run build       # compiles self-contained binary to packages/cli/dist/corpo
```

The compiled binary embeds the GUI assets and runs without Bun.

**Useful commands:**

| Command | Description |
|---|---|
| `bun run --cwd packages/cli src/index.ts --help` | List all commands (no build required) |
| `bun run --cwd packages/cli src/index.ts <cmd> --help` | Help for a specific command |

---

## Publishing
<!-- thread:4782bcd4 -->

The package follows [semver](https://semver.org).

```bash
cd packages/cli
npm pack --dry-run   # verify package contents
npm version patch    # or minor / major
npm publish
```
