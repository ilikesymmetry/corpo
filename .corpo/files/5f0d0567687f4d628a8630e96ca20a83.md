---
title: Corpo — PRD
description: Agent entrypoint for the Corpo project. Covers what Corpo is, current build status, architecture overview, key design decisions, and pointers to deeper context. Start here before working on any task.
threads:
  9ac91b73:
    author: me
    timestamp: '2026-03-09T05:47:16.873Z'
    body: |-
      I don't think a PRD should jump straight into solutions. We should first frame context and problem we are trying to solve.

      I think for us, the context is that agentic workflows are becoming more important. Problem is that our tools for human alignment and documentation are segregated from the tools we use for execution (namely claude code). Our root principle is that any important context should easily be embeddable in a local project repository so that we can operate with it while building or doing anything else agent-first.
    replies:
      - author: claude
        timestamp: '2026-03-09T06:26:37.589Z'
        body: 'Agreed. The opener should frame the tension first: agents and humans increasingly share the same working context, but our documentation layer is still designed for human-only workflows. Once that''s established, the ''what we''re building'' section lands with more weight.'
      - author: me
        timestamp: '2026-03-09T07:50:10.819Z'
        body: Let's move this section lower then and insert a "Problem Statement" section as first header.
      - author: claude
        timestamp: '2026-03-09T08:15:54.196Z'
        body: 'Acknowledged: inserting a Problem Statement as the first header, drawing from the context you described in this thread.'
      - author: claude
        timestamp: '2026-03-09T08:16:16.742Z'
        body: 'Done: inserted Problem Statement as the first section. Framed the context (agentic workflows + documentation gap), stated the problem (incomplete agent context, re-establishment cost), and restated the root principle.'
  8f8f32e1:
    author: me
    timestamp: '2026-03-09T06:24:17.629Z'
    body: I don't think an architecture section belongs in a PRD either. This is more about protocol design and the "how" of our underlying problem.
    replies:
      - author: claude
        timestamp: '2026-03-09T06:29:50.628Z'
        body: Agreed — architecture belongs in the Protocol Spec or the Architecture corpus files, not here. The PRD should stay focused on the problem and the what, leaving the how to dedicated docs.
  c3d4e5f6:
    author: claude
    timestamp: '2026-03-09T06:26:37.589Z'
    body: The 'filename is the ID' decision is especially good for agent workflows — no frontmatter parsing required to know a file's identity, and it's visible in any directory listing. It also makes refactoring the corpus (moving files, renaming groups) safe by default.
  b7a8c9d0:
    author: claude
    timestamp: '2026-03-09T06:26:37.589Z'
    body: This table will go stale as the corpus grows. The navigation config in .corpo/config.json is a better source of truth for agents — it's always current and machine-readable. Maybe the PRD should point agents there instead of maintaining a manual table.
  45a1de02:
    author: me
    timestamp: '2026-03-09T07:55:55.581Z'
    body: We don't need this section at all because of the navigation system. Delete section
    replies:
      - author: claude
        timestamp: '2026-03-09T08:16:21.780Z'
        body: 'Acknowledged: deleting the Corpus Files section — navigation in .corpo/config.json is the authoritative index.'
      - author: claude
        timestamp: '2026-03-09T08:16:41.834Z'
        body: 'Done: deleted the Corpus Files section and its table. The b7a8c9d0 anchor in that section was removed along with it.'
  c85e5811:
    author: me
    timestamp: '2026-03-09T08:04:32.430Z'
    body: Delete this H1 section title as it's redundant with the file title
    replies:
      - author: claude
        timestamp: '2026-03-09T08:14:59.654Z'
        body: 'Acknowledged: deleting the H1 title — it duplicates the file title shown in the sidebar.'
      - author: claude
        timestamp: '2026-03-09T08:15:49.625Z'
        body: 'Done: removed the H1 title from the body.'
---

Corpo is a git-backed, agent-native file protocol. Files are Markdown with
YAML frontmatter, stored in git repositories, and accessible to humans and
agents through persistent links. Designed for bottoms-up adoption — one author,
one repo, no mandate required.

---

## Problem Statement

Agentic workflows are becoming central to software development. But the tools we
use for alignment — documentation, decisions, shared context — are disconnected from
the tools we use for execution. Important context lives in Notion, Confluence, or
scattered files that agents can't reliably access or write to.

The result: agents operate with incomplete context, and humans spend time
re-establishing it at the start of every session. The gap between what a human knows
and what an agent can act on compounds over time.

**Our root principle:** any important context should live in the project repository —
versioned, addressable, and writable by both humans and agents, without any top-down
mandate required.

---

## What We're Building
<!-- thread:9ac91b73 -->

Corpo has five components, built in dependency order:

| Phase | Component | Status |
|---|---|---|
| 1 | Protocol v1.0 | in-progress |
| 2 | CLI | todo |
| 3 | Viewer | todo |
| 4 | corpo.sh backend | todo |
| 5 | Launch | todo |

**Phase 1 is the foundation.** The protocol spec must be signed off before any
implementation begins. Changes to the protocol after Phase 2 starts are costly.

Task tracker: [`tasks/index.md`](../../tasks/index.md)

---

## Architecture
<!-- thread:8f8f32e1 -->

### Files

Every Corpo file is a Markdown file with a YAML frontmatter block, named
`{file-id}.md`. The ID is a UUID v4 with hyphens stripped — 32 lowercase hex
characters. IDs are permanent; they never change even if the file moves.

Required frontmatter fields: `title`, `description`. Optional: `import` (set
when a file was created via `corpo import`, points to the original URL).

Threads live in frontmatter under a `threads` key. Each thread has an 8-char
hex ID, an author, timestamp, body, and optional replies. Thread position in
the body is marked with an HTML comment anchor: `<!-- thread:{id} -->`.

### Corpus

A corpus is a collection of files. Local files live in `.corpo/files/`. Remote
files are declared by ID in `.corpo/config.json` and fetched on demand.
`.corpo/config.json` governs the corpus with two keys:

- `remote_files` — map of `{id}: {remote}` for files fetched from elsewhere
- `navigation` — ordered group hierarchy covering local and remote files

### Cache and Sync

All fetched remote files are stored in a user-level SQLite cache at
`~/.corpo/cache.db`. The cache is binary — a file is either fully cached or
not present. Staleness is checked via blob SHA (git remotes) or API
(registry remotes). Reads are never blocked; background sync lands after.

**ID = identity. Remote = routing.** A file ID is a permanent global
reference. Where to fetch it from is a config/cache concern, never part of
the reference itself.

### Remotes

Two remote types:

- **Git remote** — `git:github.com/{owner}/{repo}.git` — fetches via the
  git host API using existing credentials
- **Registry remote** — `https://{host}` — HTTP service implementing the
  Corpo registry API (`GET /file/{id}`, `GET /file/{id}/content`)

Resolution order: cache hit → remotes in config order → error.

### Auth

Pluggable. GitHub OAuth ships first (repo visibility = file visibility).
Okta SSO and repo-level access tokens follow.

---

## Key Design Decisions
<!-- thread:c3d4e5f6 -->

- **Filename is the ID.** No `id` field in frontmatter — the filename is the
  sole source of truth for a file's identity.
- **Thread anchors are HTML comments.** `<!-- thread:{id} -->` is invisible
  to all Markdown renderers and unambiguous to parse.
- **Resolved threads live in git history only.** No sidecar archive file.
  corpo.sh backend may cache at resolution time for persistent link stability.
- **Corpus lives in `.corpo/`.** Config at `.corpo/config.json`, local files
  at `.corpo/files/`. Hidden by default; purpose-built clients are the
  intended interface.
- **No `local_files_root` in config.** Local files are always at
  `.corpo/files/` by convention. No configurable path.
- **Navigation is centralized in config.** Both local and remote files are
  placed in sidebar groups via `navigation` in `.corpo/config.json`. The first
  file in the first group is the implicit entry point for agents.
- **Cache is user-level, not project-level.** No per-corpus data store.
- **`corpo import` sets the `import` frontmatter field.** Enables dedup: CLI
  warns before creating a file with a duplicate `import` URL.

