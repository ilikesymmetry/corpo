---
title: "Session 5 — 2026-03-10"
sidebarTitle: "Session 5"
description: "Clone/Pull/Push FRD, corpo comment command and FRD, word-corpus lint rule, sidebar section nav, review and style skills, journal skill with commit-range workflow."
commit: "e8b7555d399c47564ba7be8f8f3816377f95b3dd"
---

# Session 5 — 2026-03-10

FRD authoring and review loop for Clone/Pull/Push, followed by new CLI
commands, lint rules, GUI improvements, and skills infrastructure.

### Clone/Pull/Push FRD (`4744da1`)

Wrote and iterated a Feature Requirements Doc for the `corpo clone / pull / push`
primitive — a git-style snapshot model replacing the `remote_files` live-fetch
pattern. Multiple rounds of thread resolution narrowed scope to two v1 adapters:
`path:` (local files) and `git:` (GitHub via Contents API). Key design decisions
locked in:

- `corpo clone` always creates a new file; errors if the URI is already tracked
- `corpo pull` is the sole re-sync path
- For corpo sources, the full raw source file (frontmatter and all) is used as
  the body — no fenced YAML wrapping, pull is identical for all source types
- Push to corpo targets strips local-only fields (`remote`, `syncedAt`,
  `checksum`) before writing back

Removed Google Docs and general web adapters from v1 scope entirely.

### `corpo comment` command (`8b6bf22`)

Added `packages/cli/src/commands/comment.ts` — atomically inserts a
`<!-- thread:{id} -->` anchor after a specified 1-based line in the body and
adds the matching frontmatter thread entry. Inlines lint validation before
writing; prints the generated thread ID to stdout. Registered in `index.ts`.

Also created a companion FRD (`7be90c3`) and added it to navigation under
Features/CLI.

### `word-corpus` lint rule (`7597324`)

Added a new rule to `packages/cli/src/commands/lint.ts` that flags any use of
the word "corpus" in corpo file bodies (outside fenced code blocks) and in
frontmatter text fields (title, description, thread bodies, replies). Reports
the field or 1-based body line number. Ran the style skill to clear all
existing violations across 38 files — replacements: `corpus file` → `corpo
file`, `the corpus` → `corpo`.

### Sidebar section navigation (`ecd3fff`)

The navigation sidebar now shows H1–H3 headings under the active file when
the file link is clicked while already active. Clicking a heading entry smooth-
scrolls the article to that section via a forwarded ref (`FileViewHandle`) on
`FileView`.

Thread highlight support was also broadened from headings-only to any block
immediately preceding a thread anchor. Renamed `parseHeadingThreads` →
`parseBlockThreads` (anchor-line keyed) and updated CSS classes from
`threaded-heading` to `threaded-block`.

### Skills infrastructure (`58a7616`, `e8b7555`)

Added three local skills under `skills/`:

- **`frd/`** — write or update a Feature Requirements Doc
- **`review/`** — design pass that reads a corpo file end-to-end, identifies
  3–6 substantive findings, and adds each as a thread via `corpo comment`
- **`style/`** — iteratively runs `corpo lint`, fixes all flagged issues, and
  relints until clean
- **`journal/`** (renamed from `doc-sync/`) — updated to commit outstanding
  changes with user consent, store HEAD SHA as `commit` in frontmatter, and
  scope `git log` to the range between the previous journal's `commit` and
  current HEAD

## Reflections

**corpo as a file-level git system.** Clone/pull/push makes corpo interesting
as a sync primitive at the atom level — individual files — rather than at the
repository level. A remote could theoretically serve massive volumes of files
across many repos without requiring consumers to clone entire repositories. Not
certain it's strictly better than the git model, but the scaling properties are
worth exploring as the protocol matures.

**CLIs and skills as the right abstraction ladder.** When I notice custom agent
scripts doing CRUD over corpo files, the right move is to create a CLI command.
When I notice myself instructing the agent to use specific CLI commands
repeatedly, the right move is to create a skill. Leaning into the CLI as an
abstraction layer for the agent is paying off — it's a cleaner interface than
direct filesystem manipulation and produces auditable, composable operations.

**FRDs as the standard unit of work.** The workflow of: brief chat → FRD →
author review → agent review → one more round is proving to be a tight loop.
Getting into a groove where no non-trivial feature starts without a requirements
doc. The agent review step in particular was valuable — it surfaced real design
gaps after I was already satisfied with the doc, and my replies (agreeing or
clarifying) gave the next revision meaningful signal. This workflow is starting
to look like agentation: a tight human↔agent feedback loop driven by structured
artifacts. The goal is to keep adding CLIs, skills, and UX patterns to make the
loop faster and more reliable.

## What's Next

- Implement Clone/Pull/Push (`path:` adapter first, then `git:`)
- Wire `corpo comment` into the review skill now that it's live
- Add `commit` field retroactively to earlier journals so the range lookup
  works cleanly going forward
- Consider registering local `skills/*/SKILL.md` files as project-level
  skills so they trigger automatically
