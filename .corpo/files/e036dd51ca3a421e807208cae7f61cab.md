---
title: GitHub Pages GUI
description: Feature requirements for hosting the corpo GUI as a static site on GitHub Pages, backed by the GitHub Contents API instead of a local server. Enables read and write access to any corpo repo from a browser, no CLI install required.
threads:
  '670e9799':
    author: me
    timestamp: '2026-03-10T05:16:46.833Z'
    body: Think we should call out that feature parity between local and hosted is also a goal (for now).
    replies:
      - author: claude
        timestamp: '2026-03-10T05:24:56.479Z'
        body: 'Done: added ''Feature parity with the local GUI for all supported operations'' as an explicit goal.'
  d18f5ce4:
    author: me
    timestamp: '2026-03-10T05:17:54.449Z'
    body: I also want private repo support in v1, specifically the ability for enterprise github to work with this pattern. Am happy for intermediate milestones to be on public repos for QA though.
    replies:
      - author: claude
        timestamp: '2026-03-10T05:25:35.920Z'
        body: 'Done: removed from Non-Goals and added to Goals — public repos ship first as a QA milestone, private repo + GHES support is in-scope for v1.'
  41740b56:
    author: me
    timestamp: '2026-03-10T05:20:04.852Z'
    body: Want to call out that this change motivates creating a unified layer of CRUD operations that can easily be switched between locally-running and hosted versions of the UI. It seems that this set of CRUD would mirror the same patterns for the API used by the CLI for swapping out `LocalAdapter`. Maybe there's a universal definition of CRUD interfaces that can be reused across packages to help us maintain conventions and feature parity across interfaces?
  98e112f3:
    author: me
    timestamp: '2026-03-10T05:21:04.103Z'
    body: Can we add another bullet after this that describes what the user actually does with the one-time code? It's not obvious to me. Do they open a new browser tab for the url provided and type the code there?
  79f56c8f:
    author: me
    timestamp: '2026-03-10T05:21:49.049Z'
    body: Also need to define how allowlists can be configured for repos to gate who has write access (additional filter on top of people who login with github).
  fe961d7e:
    author: me
    timestamp: '2026-03-10T05:22:36.976Z'
    body: I'm not understanding this section. Can you elaborate on what would need to be true about our final implementation as an invariant?
  e2aaa132:
    author: me
    timestamp: '2026-03-10T05:23:10.759Z'
    body: Corpo init should scaffold actions workflow
  f3c00698:
    author: me
    timestamp: '2026-03-10T05:23:46.170Z'
    body: I'm not understanding this, can you elaborate?
---

## Overview

The corpo GUI should be deployable as a static site on GitHub Pages, hosted directly from any corpo repo. Users who haven't installed the CLI can still read and comment on corpo files from a browser. The GitHub Contents API replaces the local server as the data layer.

## Goals
<!-- thread:670e9799 -->

- Any corpo repo gets a hosted GUI at `{owner}.github.io/{repo}/` automatically
- Read-only access works with no auth (public repos)
- Authenticated users can add and reply to threads, persisted as commits
- Feature parity with the local GUI for all supported operations (reading, threading)
- Write access is controlled by an allowlist configured in `.corpo/config.json`
- Private repo support, including enterprise GitHub (GHES) — public repos ship first as an intermediate QA milestone
- No backend infrastructure required

## Non-Goals

- File creation, deletion, or navigation reordering (write-heavy operations stay CLI-only for now)
- Real-time collaboration / conflict resolution UI
<!-- thread:d18f5ce4 -->

## Data Layer
<!-- thread:41740b56 -->

This feature motivates a **unified adapter interface** for all corpo CRUD operations — a shared TypeScript interface (e.g. `CorpoAdapter`) implemented by both a `LocalAdapter` (wrapping the local server) and a `GitHubAdapter` (wrapping the GitHub Contents API). The GUI switches adapters at runtime based on context; the CLI already uses an analogous pattern. Defining this interface as a shared type across packages enforces feature parity and prevents the two surfaces from drifting. Any operation added to one adapter must be added to the other.

Replace `fetch('/api/*')` calls in `api.ts` with GitHub Contents API calls:

| Operation | Endpoint |
|---|---|
| Read navigation | `GET /repos/{owner}/{repo}/contents/.corpo/config.json` |
| Read file | `GET /repos/{owner}/{repo}/contents/.corpo/files/{id}.md` |
| Write file (thread add/resolve) | `PUT /repos/{owner}/{repo}/contents/.corpo/files/{id}.md` |

All responses return base64-encoded content — decoded client-side. The file SHA returned on read must be included on write to prevent conflicts. Simultaneous writes get a 409; retry after re-fetching.

The local server mode is preserved. A `VITE_GITHUB_OWNER` / `VITE_GITHUB_REPO` env var pair (set at build time) selects GitHub API mode. If unset, the app falls back to the current local server behavior.

## Auth
<!-- thread:79f56c8f -->

Use **GitHub OAuth Device Flow** — no backend or redirect URI required:

1. POST to `github.com/login/device/code` with the OAuth App `client_id`
2. Show user the one-time code and `github.com/activate` URL
<!-- thread:98e112f3 -->
3. Poll `github.com/login/oauth/access_token` until authorized
4. Store token in `localStorage`; send as `Authorization: Bearer {token}` on writes

Token scope needed: `public_repo` (for public repos). The OAuth App `client_id` is public — safe to embed in the bundle.

Unauthenticated users get read-only access. Write UI elements are hidden or disabled until a token is present.

## Routing
<!-- thread:fe961d7e -->

Switch from browser history to **hash routing** (`/#/files/{id}`). GitHub Pages serves only `index.html` at the root — deep links via history routing return 404. Hash routing requires no server configuration.

## Deployment

A GitHub Actions workflow in each corpo repo handles deployment:

```
on: push (main branch)

jobs:
  deploy:
    - bun install
    - vite build  (with VITE_GITHUB_OWNER, VITE_GITHUB_REPO set)
    - deploy dist/ to gh-pages branch via actions/deploy-pages
```

The workflow file is scaffolded by `corpo init` so new repos get it automatically.

## Open Questions

- Should `corpo init` scaffold the Actions workflow, or is that a separate `corpo pages` command?
<!-- thread:e2aaa132 -->
- Device flow UX is clunky — worth a Cloudflare Worker for proper OAuth redirect in v2?
- How do we handle the `base` path in `vite.config.ts` generically across repos with different names?
<!-- thread:f3c00698 -->
